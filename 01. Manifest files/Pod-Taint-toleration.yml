# kubectl taint nodes node1 app=myapp:NoSchedule

apiVersion:
kind: Pod
metadata:
  name: myapp-pod
spec:
  containers:
  - name: nginx-container
    image: nginx

  tolerations:
  - key:" "
    operator:"Equal"
    value:" "
    effect:" "